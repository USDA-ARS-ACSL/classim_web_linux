# USDA eAuth OIDC Configuration Example
# Copy this to .env and update with your actual values

# Required OIDC Configuration
OIDC_CLIENT_ID=your-usda-eauth-client-id
OIDC_PRIVATE_KEY_PATH=./private_key.pem
# OR embed the private key directly (use one or the other):
# OIDC_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----"

# Application URLs
OIDC_REDIRECT_URI=https://yourdomain.com/api/v1/auth/callback

# Admin users (comma-separated email addresses that get automatic admin privileges)
ADMIN_EMAILS=admin1@usda.gov,admin2@usda.gov,your.email@usda.gov

# Application Environment
ENVIRONMENT=staging  # local, staging, or production
DOMAIN=yourdomain.com
PROJECT_NAME=CLASSIM Web Application

# Security
SECRET_KEY=your-super-secure-secret-key-change-this-in-production

# Database Configuration
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=classim
POSTGRES_PASSWORD=your-database-password
POSTGRES_DB=classim

# CORS Origins (if needed)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Email Configuration (optional)
SMTP_TLS=true
SMTP_SSL=false
SMTP_PORT=587
SMTP_HOST=smtp.usda.gov
SMTP_USER=your.email@usda.gov
SMTP_PASSWORD=your-smtp-password
EMAILS_FROM_EMAIL=noreply@usda.gov
EMAILS_FROM_NAME=CLASSIM Application

# Sentry (optional, for error tracking)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id